# geocaching-cli

Personal command-line tool for Geocaching (offline GPX/PQ first, optional live commands).

This repository is a bootstrap. Implementation lands on a follow-up branch.

## Secrets

Never commit Geocaching credentials. Use environment variables or a gitignored local config.
